# I think pip install pyjwt.
# There are multiple libraries that go by 'jwt' as the module name, so you may want to run this in a venv.
##TODO
# Print off all the possible content types with a token according to the RFC and what they mean.
# Take user input

import jwt
f = "#THE JWT Token#
jwt.decode(f, verify=False)
